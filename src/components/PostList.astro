---
import PostRow from './PostRow.astro';
import type { SearchDocument } from '../utils/search';

interface Props {
  documents: SearchDocument[];
  interactive?: boolean;
}

const { documents, interactive = false } = Astro.props;
---

<div class="directory-list" data-directory-list={interactive ? true : undefined}>
  {documents.map((document) => (
    <PostRow document={document} interactive={interactive} />
  ))}
</div>
